import { NotFoundDisplay } from "@/components/shared";

export default function NotFound() {
  return (
    <div className="relative min-h-screen bg-background">
      <NotFoundDisplay />
    </div>
  );
}
